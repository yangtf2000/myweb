# YTF 工具箱 · 网站风格准则

> 统一设计语言：深色 · 活泼 · 科技感。前后台一致，分类可微调。
> 单一风格源：`assets/theme.css`（变量与组件）+ `assets/nav.js`（导航/页脚注入）。

## 1. 设计基调
- **深色为底**：近黑深蓝 `#0b1120`，叠加三处彩色径向光晕（天蓝 / 靛蓝 / 翠绿），营造科技氛围。
- **活泼**：强调色用渐变、玻璃拟态卡片、圆角、轻微悬浮动效。
- **科技感**：等宽字体用于代码、发光描边、渐变文字标题。

## 2. 设计 Token（CSS 变量，改一处全局生效）
| 变量 | 值 | 说明 |
|---|---|---|
| `--bg` | `#0b1120` | 页面底色 |
| `--text` | `#e8ecf4` | 主文字 |
| `--muted` | `#9aa6bf` | 次要文字 |
| `--card-bg` | `rgba(255,255,255,0.05)` | 玻璃卡片底 |
| `--card-border` | `rgba(255,255,255,0.09)` | 卡片描边 |
| `--radius` | `18px` | 大圆角 |
| `--maxw` | `1080px` | 内容最大宽度 |
| `--nav-h` | `62px` | 顶栏高度 |
| `--accent1/2/3` | 见分类 | 强调色三元组 |
| `--glow1/2/3` | 见分类 | 背景光晕色 |
| `--font` | 系统 + 苹方/雅黑 | 中文优先无衬线 |
| `--mono` | Fira Code 等 | 代码字体 |

## 3. 分类主题（基调一致，侧重不同）
通过 `<body class="theme-*">` 切换（由 `nav.js` 依据 `data-theme` 自动加）：
| 分类 | class | 强调色 | 气质 |
|---|---|---|---|
| 通用 / 工作 | `theme-work` | 钢蓝 `#64748b` + 天蓝 + 靛蓝 | **更庄重**，光晕收敛，标题不用彩色渐变 |
| 城市规划 | `theme-city` | 蓝 `#60a5fa` + 青 `#22d3ee` | 蓝图科技感 |
| 学习成长 | `theme-study` | 翠绿 `#34d399` + 青绿 | 清新有活力 |
| 生活 | `theme-life` | 琥珀 `#fbbf24` + 玫红 `#fb7185` | 温暖活泼 |
| 小游戏 | `theme-game` | 粉 `#f472b6` + 紫 `#a855f7` | 玩味鲜艳 |
| 首页 | `theme-home` | 靛蓝 + 天蓝 + 翠绿 | 默认活泼 |

## 4. 组件规范
- **顶部导航 `.site-nav`**：玻璃拟态 sticky 顶栏；左品牌 `YTF 工具箱`，右分类入口；移动端折叠为汉堡菜单。
- **页脚 `.site-footer`**：居中，文案 `© 2026 · by ytf000@sina.com`。
- **内容容器 `.ytf-page`**：最大宽 `1080px` 居中，标题用渐变文字，二级标题用左侧渐变竖条。
- **按钮 `.btn` / `.btn.ghost`**：主按钮渐变填充，幽灵按钮描边。
- **卡片 `.ytf-card`**：玻璃卡片，悬浮上浮 + 阴影。
- 所有页面正文前预留 `--nav-h` 顶 padding，避免被顶栏遮挡。

## 5. 响应式
- 断点 `760px`：导航折叠为汉堡下拉；标题字号缩小；容器内边距收窄。
- 卡片网格 `auto-fill minmax(220px,1fr)` 自适应换行。
- 触摸目标 ≥ 40px，便于手机点击。

## 6. 部署与相对路径约定
- 站点根 = 仓库根（`D:\myweb`）。共享资源放 `assets/`。
- 每个页面用 `nav.js` 的 `data-root` 指回根目录：
  - `pages/xxx/index.html` → `data-root="../"`（实际需 `../../`，见下注）
  - `pages/xxx/sub/index.html` → `data-root="../../../"`
- **注**：从 `pages/X/index.html` 回到仓库根需 `../../`（上两级：X → pages → 根）。脚本已按文件深度自动计算，无需手填。
- 全站使用相对链接，可直接 `github.io` 用户站（根 `/`）或项目子路径（如 `/ai888/`）部署，不写死绝对路径。

## 7. 文章页面规范（研究/教程类长文）
适用于深度研究文章、教程指南等长篇内容页面（如"原拆原建"页面）。

### 7.1 页面结构
```
┌─────────────────────────────────────┐
│  顶部导航栏（nav.js 自动注入）        │
├──────────┬──────────────────────────┤
│          │  文章标题（渐变文字）      │
│          │  元信息栏（日期/类型/字数） │
│  左侧    │  ─────────────────────   │
│  固定    │  摘要块（蓝色左边框）      │
│  侧边    │  关键词标签（圆角标签）    │
│  栏目录  │  ─────────────────────   │
│          │  正文内容                  │
│          │  - h2：左侧蓝色竖线+底边线  │
│          │  - h3：左侧彩色竖线        │
│          │  - 表格：深色渐变表头       │
│          │  - 提示框：info/warning/   │
│          │    success 三种样式        │
│          │  ─────────────────────   │
│          │  参考文献 / 结语           │
└──────────┴──────────────────────────┘
  右下角：返回顶部按钮
  顶部：阅读进度条（3px渐变）
```

### 7.2 核心组件
| 组件 | 类名/选择器 | 样式要点 |
|---|---|---|
| **左侧目录** | `.sidebar` / `.toc` | 固定定位，宽度240px，层级导航，当前章节高亮，滚动自动高亮 |
| **阅读进度条** | `.progress-bar` | 顶部固定，3px高，蓝青渐变，随滚动宽度变化 |
| **返回顶部** | `.back-to-top` | 右下角固定，圆形渐变按钮，滚动超过500px显示 |
| **摘要块** | `.abstract` / `.summary` | 蓝色左边框（4px），深色背景，圆角8px，内边距20px |
| **关键词标签** | `.keyword-tag` | 圆角20px，蓝色半透明背景，白色文字，左右内边距14px |
| **元信息栏** | `.meta-bar` | 图标+文字，灰色小字，居中排列，分隔线分隔 |

### 7.3 正文排版
- **h1 标题**：渐变文字（蓝→青），字号32-36px，居中，字重700
- **h2 标题**：左侧4px蓝色竖线 + 底部1px渐变边线，字号24px，上边距40px
- **h3 标题**：左侧4px彩色竖线（按主题色），字号18px，上边距28px
- **h4 标题**：加粗，字号16px，上边距20px
- **正文段落**：行高1.8，字号15-16px，段间距16px
- **列表**：ul 用彩色圆点，ol 用彩色数字，缩进20px，项间距8px

### 7.4 表格样式
- 表头：深色渐变背景（#0f172a → #1e293b），白色文字，底部2px主题色边线
- 表体：斑马纹（奇数行#1e293b，偶数行#0f172a）
- 边框：1px #334155 分隔线
- 悬停：行高亮（背景透明度增加）
- 圆角：8px，溢出隐藏

### 7.5 提示框（Callout）
三种类型，统一左边框3px + 半透明背景 + 圆角8px：
| 类型 | 左边框色 | 背景色 | 图标 | 用途 |
|---|---|---|---|---|
| **info** | 蓝 #3b82f6 | rgba(59,130,246,0.1) | 💡 | 说明、提示、知识点 |
| **warning** | 黄/红 #f59e0b / #ef4444 | rgba(245,158,11,0.1) | ⚠️ | 注意、警告、避坑 |
| **success** | 绿 #22c55e | rgba(34,197,94,0.1) | ✅ | 成功、结论、正面 |

### 7.6 交互功能
1. **目录自动高亮**：滚动时根据视口位置自动高亮当前章节对应的目录项
2. **平滑滚动**：点击目录项平滑滚动到对应位置
3. **返回顶部**：点击平滑滚动到顶部
4. **阅读进度**：顶部进度条随滚动实时更新
5. **移动端适配**：<760px时侧边栏隐藏，点击汉堡菜单滑出

### 7.7 公众号版本（深色主题）
- 背景：深蓝渐变（#0f172a → #1e293b）
- 卡片：深色背景 + 1px边框 + 12px圆角
- 标题：彩色顶部边框（3px），居中
- 强调：渐变文字（橙→青）
- 表格：深色表头，斑马纹
- 提示框：左边框彩色 + 半透明背景
- 宽度：适配手机屏幕（375px基准）
- 所有样式内联（兼容微信公众号编辑器）

## 8. 维护方式
- 改配色 / 圆角 / 导航项 → 只改 `assets/theme.css` 或 `assets/nav.js`，全站生效。
- 新增页面：拷贝任一页面结构，设好 `data-theme`，引入 `theme.css` 与 `nav.js` 即可。
- 新增文章页：参考 `pages/yuanchaiyuanjian/index.html` 结构，复用侧边栏、进度条、返回顶部等组件。
- 作者署名 `杨铁夫` 已从页面移除，统一以 `by ytf000@sina.com` 落款。
